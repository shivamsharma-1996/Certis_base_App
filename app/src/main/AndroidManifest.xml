<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.certis_base_app">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.onboarding.registration.RegisterStep1Step2Activity_"
            android:screenOrientation="landscape" />
        <activity
            android:name=".ui.onboarding.registration.RegisterStep3Activity_"
            android:configChanges="orientation|screenSize"
            android:theme="@style/FullscreenTheme"/>
        <activity
            android:name=".ui.onboarding.registration.CameraPreviewSubmitActivity_"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.onboarding.login.LandingActivity_"
            android:screenOrientation="landscape"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.onboarding.registration.RegisterStep4Activity_"
            android:screenOrientation="landscape">

        </activity>
    </application>
</manifest>


















